export const DataTypes = {
    PRODUCTS: "products",
    CATEGORIES: "categories"
}

export const ActionTypes = {
    DATA_LOAD: "data_load"
}

// There are two data types --PRODUCTS and CATEGORIES--and
// a single action, DATA_LOAD, which will populate the data
// store